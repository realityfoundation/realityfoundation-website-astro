---
// Shared block — section (d), Variant 1: non-profit, no equity.
// Salary band is derived from frontmatter props — never hardcoded.

interface Props {
  salaryMin?: number;
  salaryMax?: number;
  salaryCurrency?: string;
  salaryPeriod?: "year" | "month";
  salaryNote?: string;
}

const {
  salaryMin,
  salaryMax,
  salaryCurrency = "EUR",
  salaryPeriod = "year",
  salaryNote,
} = Astro.props;

function formatSalary(
  min?: number,
  max?: number,
  currency: string = "EUR",
  period: string = "year",
  note?: string,
): string {
  if (min === undefined) return "Salary band shared during the process";
  const sym = currency === "EUR" ? "€" : currency;
  const fmt = (n: number) => n.toLocaleString("en-US");
  let s = `${sym}${fmt(min)}`;
  if (max !== undefined) s += ` – ${sym}${fmt(max)}`;
  s += ` / ${period}`;
  if (note) s += `  ${note}`;
  return s;
}

const salary = formatSalary(salaryMin, salaryMax, salaryCurrency, salaryPeriod, salaryNote);
---

<section class="wwo-section">
  <h2 class="wwo-heading">What we offer</h2>
  <ul class="wwo-list">
    <li><strong>{salary}</strong> gross on a Belgian permanent contract. We publish the band because you should not have to negotiate blind</li>
    <li><strong>Brussels or remote</strong> Our base is Brussels, and we can hire anywhere in the EU. We also bring the team together 2 times a year, on us</li>
    <li><strong>Tools and learning budget</strong> A real budget for equipment, training, and relevant conferences</li>
    <li><strong>Your work is public</strong> What you build here stays in the open, under your name</li>
    <li><strong>Growth, impact, and ownership</strong> You can grow into the role and shape how it evolves, while working with a small dynamic team on a mission that protects trust in digital media and lets you be part of meaningful change</li>
  </ul>
</section>

<style>
  .wwo-section {
    border-top: 1px solid var(--color-secondary);
    padding-top: 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .wwo-heading {
    font-family: var(--font-plex);
    font-size: 1.25rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: -0.02em;
    color: var(--color-foreground);
  }

  .wwo-list {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    font-family: var(--font-geist);
    font-size: 1rem;
    line-height: 1.38;
    color: var(--color-secondary);
    list-style: disc;
    padding-left: 1.25rem;
    margin: 0;
  }

  .wwo-list strong {
    color: var(--color-foreground);
    font-weight: 600;
  }
</style>
